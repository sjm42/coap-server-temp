# Cargo.toml

[package]
name = "coap_server_temp"
version = "0.4.3"
authors = ["Sami J. Mäkinen <sjm@iki.fi>"]
edition = "2021"
resolver = "2"


[dependencies]
anyhow = "1.0"
chrono = "0.4"

# coap-server = "0.1"
coap-server = { git = "https://github.com/sjm42/coap-server-rs.git" }
# coap-server = { path = "../coap-server-rs" }

# coap-lite = "=0.9.1" # keep in sync with dependency
coap-lite = "0.11" # keep in sync with dependency
# coap-lite = { git = "https://github.com/martindisch/coap-lite.git" }

env_logger = "0.10"
futures = "0.3"
influxdb2 = "0.4"
log = "0.4"
once_cell = "1.17"
structopt = "0.3"
tokio = "1.28"

# [patch.crates-io]
# coap-lite = { git = "https://github.com/martindisch/coap-lite.git" }

[build-dependencies]
build-data = "0.1"


[profile.release]
lto = true
opt-level = 3 #default
# opt-level = 'z'  # Optimize for size.

# EOF
