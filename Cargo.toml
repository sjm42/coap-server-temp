[package]
name = "coap-server-temp"
version = "0.1.1"
authors = ["Sami J. Mäkinen <sjm@iki.fi>"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
anyhow = "1"
chrono = "0"
coap = "0"
coap-lite = "0.7.0"   # keep in sync with dependency
env_logger = "0"
influxdb-client = "0"
log = "0"
parking_lot = "0"
structopt = "0"
tokio = "1"

[build-dependencies]
build-data = "0"

[profile.release]
lto = true
opt-level = 3 #default
# opt-level = 'z'  # Optimize for size.

# EOF
